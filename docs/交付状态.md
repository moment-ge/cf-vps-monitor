# 交付状态

更新时间：2026 年 9 月 5 日。

## 已完成

- 源码位于当前项目，参考上游已拉取到 `upstream/`，提交版本见 README。
- 公开监控页面、管理后台、独立节点密钥、告警事件、费用与到期管理已经实现。
- 低配置探针使用单个 Go 二进制，默认 120 秒上报、900 秒历史采样，针对更多节点自动扩大间隔。
- Linux 4 种架构、Windows 2 种架构和 macOS 2 种架构的二进制已生成。
- 20 项本地 API 与状态测试、Go 单元测试、TypeScript 检查、生产构建及部署 dry-run 已通过。
- 已按参考站更新为深色运维监控台：纯 CSS 网格背景、分层卡面、四列桌面卡片和手机单列；首页默认展示在线节点，整卡可点击查看详情，加入可旋转地区球体、在线数/总数标记和首报检测。节点支持地区参考点或管理员指定经纬度，位置在详情与后台管理中展示，详细记录见 `docs/界面与接入更新.md`。
- 101 台同时异常与恢复的维护任务已验证，单次 SQL 数量控制在 D1 Free 限制以内；保留期缩短时分批清理历史。
- 最终补充修复：网卡名称无效时报错、活动节点数与管理操作一起提交、保留管理员原始设置并单独计算当前规模下的有效间隔。
- 部署与使用手册包含免费配额估算、3x-ui 共存、三种系统安装、备份、升级和卸载步骤。

## 交付文件

- 完整压缩包：`release/cf-vps-monitor-v1.0.0.zip`
- 压缩包校验：`release/SHA256SUMS`
- 探针二进制及校验：`release/agent/`
- 中文手册：`docs/部署与使用手册.md`
- 本机预览：`http://127.0.0.1:8790`。这是本地地址，仅本机运行时可用。

压缩包不包含真实密钥、Cloudflare 认证、本地数据库或演示数据。演示节点不持续上报，稍后显示离线属于正常行为。

## 尚需外部条件

**Cloudflare 尚未上线。** 本机 `wrangler whoami` 明确返回未认证，没有远程数据库 ID，也没有创建线上资源。登录需由账户持有人在浏览器完成。在项目目录执行 `npx wrangler login` 后，按手册第 5 节创建免费 D1、设置生产 Secrets 并部署。无需发送密码或 Token 到聊天。

尚未提供 VPS 访问权限，因此不能执行真实 Linux 512 MB 或 Windows 常驻验收。本机 macOS 的短时 RSS 约 13.1 MiB，不应当作所有平台常驻占用的保证。Cloudflare 免费版线上 CPU 与读写额度也必须在首次接入后观察。

没有 Telegram 凭据，没有发送真实通知。当前版本已完成类型、API、构建检查，以及桌面和手机浏览器验收。真实 macOS 单次上报已在新版接入弹窗确认成功。

## 自动续办

已建立本任务的自动续办。后续运行检查已有认证和剩余可执行工作；完成后暂停。若仅剩账户登录，也会记录状态并暂停，避免持续重复检查。不会自动开通付费套餐。
